// TODO: This component needs to be rewritten to match the actual schema
// Current issues:
// - formData has fields not in schema (sdsFileId, sdsVersion, sdsIssuedAt, isDraft)
// - SDS is a separate table, not part of products
// - Need to reconcile with actual Convex schema

export default function ProductForm() {
  return <div>Product form - not yet implemented</div>;
}
